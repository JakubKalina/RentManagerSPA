import React from "react";

const SearchPageContainer: React.FC<{}> = () => {
    // const Content = (

    // );
    return (
        <div>
            <h1>Strona wyszukiwania użytkowników</h1>
            
        </div>
    );
};

export default SearchPageContainer;